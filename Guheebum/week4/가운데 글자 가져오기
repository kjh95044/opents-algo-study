function solution(s) {
    const mid = Math.floor(s.length / 2);

    if (s.length % 2 === 1) {
        return s[mid];        // 홀수: 가운데 1글자
    } else {
        return s[mid - 1] + s[mid];  // 짝수: 가운데 2글자
    }
}
