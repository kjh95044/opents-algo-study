function solution(n) {
    // x는 1부터 시작해서 n까지 확인합니다.
    // n을 x로 나눈 나머지가 1이 되는 가장 작은 x를 찾으면 됩니다.
    for (let x = 1; x < n; x++) {
        if (n % x === 1) {
            return x; // 조건을 만족하는 가장 작은 x를 반환합니다.
        }
    }
}
